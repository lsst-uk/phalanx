apiVersion: v1
kind: ConfigMap
metadata:
  name: "repertoire"
  labels:
    {{- include "repertoire.labels" . | nindent 4 }}
  annotations:
    argocd.argoproj.il/sync-wave: "-1"
data:
  config.yaml: |
    {{- toYaml .Values.config | nindent 4 }}
